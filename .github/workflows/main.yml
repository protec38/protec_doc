name: Génération du PDF

on: push

jobs:
  convert_via_pandoc:
    runs-on: ubuntu-18.04
    steps:
      - uses: docker://pandoc/core:2.9
        with:
          args: "-f commonmark -o statuts.pdf statuts.md" # gets appended to pandoc command
